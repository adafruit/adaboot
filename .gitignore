outdir/
.*.swp
target.sh
*.pyc
tags
rusty-tags.*

# mynewt
/repos/
/project.state
/bin/
/targets/
**/build/**/*

#Eclipse project files
.cproject
.project

# Clangd creates this to cache its results
.cache/

# The code cache is a transient file.
compile_commands.json

# Compiled python modules.
*.pyc

# Setuptools distribution folder.
/scripts/dist/

# Python egg metadata, regenerated from source files by setuptools.
/scripts/*.egg-info
/scripts/*.egg

# The target directory from Rust development
/target/

# Standalone Zephyr build workspace (see Makefile / zephyr-config/west.yml.in).
# `make workspace` fetches Adafruit Zephyr + HALs here; this repo itself is the
# mcuboot module, so none of these are committed.
.west/
deps/
build-*/
zephyr-config/west.yml
