# SPDX-License-Identifier: Apache-2.0
#
# The DK1 has no push button; UF2 entrance uses the joystick selector button
# on PA3 (mcuboot-button0 in dts/st/stm32wba65i_dk1.dtsi), which needs the
# GPIO driver. prj.conf leaves GPIO off to save code size.
CONFIG_GPIO=y