# Build the Adaboot bootloader and its slot0 bootloader-updater for every
# board this fork owns a partition layout for (tools/boards.toml).
#
# The board matrix is generated from `tools/standalone_build.py list` (the
# same source the Makefile uses) so the workflow cannot drift from the dts/
# tree. Each matrix job sets up a west workspace with the Adafruit Zephyr
# fork via zephyrproject-rtos/action-zephyr-setup (the same action
# CircuitPython's zephyr-cp port CI uses, against the same zephyr-config
# manifest) and then runs `make build` / `make updater`. Boards whose
# bootloader is UF2-capable (conf/<key>.conf enables CONFIG_MCUBOOT_UF2=y,
# listed by `tools/uf2_updater.py list`) additionally get `make uf2` so their
# updater ships as a .uf2 you can drag onto the bootloader's UF2 drive.
#
# Artifacts per board:
#   mcuboot-<board>/mcuboot.bin                    - the bootloader
#   updater-<board>/zephyr.signed.bin              - slot0 image that rewrites
#                                                    the boot partition
#   uf2-<board>/mcuboot-updater.uf2                - the updater as a UF2 file
#                                                    (UF2-capable boards only)

name: Build Adaboot

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

# Only cancel ongoing runs for PRs
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  boards:
    name: List boards
    runs-on: ubuntu-24.04
    outputs:
      matrix: ${{ steps.boards.outputs.matrix }}
      uf2: ${{ steps.boards.outputs.uf2 }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false
          show-progress: false
          fetch-depth: 1
      - name: Build board matrix
        id: boards
        run: |
          mapfile -t boards < <(python3 tools/standalone_build.py list)
          if [ "${#boards[@]}" -eq 0 ]; then
            echo "No boards found" >&2
            exit 1
          fi
          matrix=$(python3 -c 'import json, sys; print(json.dumps({"board": sys.argv[1:]}))' "${boards[@]}")
          echo "matrix=$matrix" >> "$GITHUB_OUTPUT"
          # UF2-capable boards (conf/<key>.conf enables CONFIG_MCUBOOT_UF2=y):
          # these get an extra `make uf2` step + a .uf2 artifact.
          mapfile -t uf2_boards < <(python3 tools/uf2_updater.py list)
          uf2=$(python3 -c 'import json, sys; print(json.dumps(sys.argv[1:]))' "${uf2_boards[@]}")
          echo "uf2=$uf2" >> "$GITHUB_OUTPUT"

  build:
    name: ${{ matrix.board }}
    needs: boards
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.boards.outputs.matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false
          show-progress: false
          fetch-depth: 1

      - name: Generate west manifest
        run: make zephyr-config/west.yml

      - name: Set up Zephyr
        uses: zephyrproject-rtos/action-zephyr-setup@v1
        with:
          app-path: zephyr-config
          toolchains: arm-zephyr-eabi

      - name: Build bootloader
        run: make build BOARD=${{ matrix.board }}

      - name: Build updater
        run: make updater BOARD=${{ matrix.board }}

      - name: Build UF2 updater
        if: contains(fromJSON(needs.boards.outputs.uf2), matrix.board)
        run: make uf2 BOARD=${{ matrix.board }}

      - name: Upload bootloader
        uses: actions/upload-artifact@v7
        with:
          name: mcuboot-${{ matrix.board }}
          path: build-${{ matrix.board }}/mcuboot.bin
          if-no-files-found: error

      - name: Upload updater
        uses: actions/upload-artifact@v7
        with:
          name: updater-${{ matrix.board }}
          path: build-${{ matrix.board }}-updater/zephyr/zephyr.signed.bin
          if-no-files-found: error

      - name: Upload UF2 updater
        if: contains(fromJSON(needs.boards.outputs.uf2), matrix.board)
        uses: actions/upload-artifact@v7
        with:
          name: uf2-${{ matrix.board }}
          path: build-${{ matrix.board }}-updater/mcuboot-updater.uf2
          if-no-files-found: error